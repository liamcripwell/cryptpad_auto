from setuptools import setup, find_packages

setup(
    name="cryptpad_generation",
    version="0.0.1",
    author="Liam Cripwell",
    description="A tool to automate the generation of CryptPad forms.",
    packages=find_packages(),
    include_package_data=True,
)
